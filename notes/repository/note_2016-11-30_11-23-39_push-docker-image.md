# Pousser une image Docker vers le Docker Hub

Créer un compte Docker, un dépôt puis:

	$ docker tag 08522000 remipassmoilesel/project:latest
	$ docker login
	$ docker push remipassmoilesel/project


